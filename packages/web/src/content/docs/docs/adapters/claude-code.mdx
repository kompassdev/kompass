---
title: Claude Code
description: Status of planned Claude Code adapter support.
---

Claude Code adapter support is planned but not shipped yet.

The current direction is to keep Kompass as a shared core toolkit with adapter-specific packages so the same workflows can be reused across agent surfaces instead of being rewritten per platform.

For now, OpenCode is the supported adapter documented in this site.
