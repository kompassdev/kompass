---
title: /reload
description: Reload the current OpenCode project cache after config or template changes.
---

## Purpose

Use `/reload` after changing Kompass config, commands, agents, or tools.

## Usage

```text
/reload
```

## Typical flow

- refresh the OpenCode project cache
- make the new config active without restarting the app

## Common tools

- `reload`
