---
title: /skill/create
description: Create a focused Agent Skill from repository context and user direction.
---

## Purpose

Use `/skill/create` to author a new reusable Agent Skill that is grounded in the current project instead of generic best-practice filler.

## Usage

```text
/skill/create [skill-direction]
```

## Typical flow

- inspect existing skills, skill roots, and nearby project artifacts
- stop if the request would duplicate an existing skill without a clear scope boundary
- derive a valid hyphenated skill name and default root such as `.agents/skills/`
- choose a supported model or user invocation mode based on who must discover the skill
- create a concise `SKILL.md` with checkable steps and one source of truth for each instruction
- move branch-only detail into support files with explicit load conditions
- validate invocation frontmatter, completion criteria, and file references

## Common tools

- `glob`
- `read`
- file edit tools
- relevant validation commands when they exist
