---
title: commit
description: Shared partial for commit message formatting and commit execution.
---

## Behavior

- prefers conventional commit subjects
- uses a subject-plus-bullets format for non-trivial changes
- stages changes with `git add`
- stores `<commit-message>` and `<hash>`
- only falls back to `git status` when commit creation fails
